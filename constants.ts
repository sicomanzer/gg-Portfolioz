export const DEFAULT_GROWTH = 3.0;
export const DEFAULT_REQ_RETURN = 10.0;

export const INITIAL_PORTFOLIO_SETTINGS = {
  totalCapital: 1000000,
  companyCount: 5,
};

export const MOCK_DELAY_MS = 1200;